{
  "name": "cypress_test",
  "version": "1.0.0",
  "description": "Cypress test for Senior QA Challenge",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open --browser chrome --e2e --config baseUrl=http://localhost:3000/weather",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bhavish",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.13.0",
    "cypress-browser-permissions": "^1.1.0"
  }
}
